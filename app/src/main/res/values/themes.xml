<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.TrickTrack" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/cool_teal</item>
        <item name="colorPrimaryVariant">@color/cool_teal</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <!--<item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>-->
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="android:textColorHighlight">@color/text_highlight_teal</item>
        <item name="colorAccent">@color/cool_teal</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.Splash_Screen" parent="Theme.TrickTrack">
        <item name="android:windowBackground">@drawable/splash_screen</item>
        <item name="android:statusBarColor">@color/black</item>
    </style>

    <style name="CustomSearchViewStyle" parent="Widget.AppCompat.SearchView">
        <!-- Change the hint color to light grey -->
        <item name="android:queryHint">@color/light_grey</item>
        <item name="android:editTextColor">@color/black</item>
        <item name="android:textColorHint">@color/light_grey</item>
        <item name="queryHint">@color/light_grey</item>
        <item name="submitBackground">@color/transparent</item>
        <item name="queryBackground">@color/transparent</item>
        <!-- Set the search icon and close icon to transparent -->
        <item name="searchIcon">@android:color/transparent</item>
        <item name="closeIcon">@android:color/transparent</item>
    </style>

    <style name="WhiteMaterialButtonBackground">
        <item name="colorPrimary">@color/white</item>
        <item name="colorPrimaryVariant">@color/white</item>
        <item name="colorOnPrimary">@color/white</item>
    </style>





</resources>